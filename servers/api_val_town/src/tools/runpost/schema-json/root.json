{
  "type": "object",
  "properties": {
    "valname": {
      "description": "Name of the val, in concatenated style, like username.valname",
      "pattern": "^[^.]+.[^.]+$",
      "type": "string"
    },
    "args": {
      "type": "array",
      "items": {}
    }
  },
  "required": [
    "valname"
  ]
}