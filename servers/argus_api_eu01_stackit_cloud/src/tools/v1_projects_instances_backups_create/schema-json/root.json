{
  "type": "object",
  "properties": {
    "instanceId": {
      "type": "string"
    },
    "projectId": {
      "type": "string"
    },
    "backupTarget": {
      "description": "List of backup targets",
      "items": {
        "enum": [
          "alertConfig",
          "alertRules",
          "scrapeConfig",
          "grafana"
        ],
        "type": "string"
      },
      "type": "array"
    },
    "Authorization": {
      "description": "Accepts technical credentials and api gateway access.",
      "type": "string"
    }
  },
  "required": [
    "instanceId",
    "projectId",
    "Authorization"
  ]
}