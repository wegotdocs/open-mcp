{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the file operation.",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ]
}