{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the OAuth client.",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ]
}