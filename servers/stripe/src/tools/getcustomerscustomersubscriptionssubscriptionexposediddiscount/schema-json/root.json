{
  "type": "object",
  "properties": {
    "customer": {
      "maxLength": 5000,
      "type": "string"
    },
    "subscription_exposed_id": {
      "maxLength": 5000,
      "type": "string"
    },
    "expand": {
      "description": "Specifies which fields in the response should be expanded.",
      "items": {
        "maxLength": 5000,
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "customer",
    "subscription_exposed_id"
  ]
}