{
  "description": "Only return prices with these recurring fields.",
  "properties": {
    "interval": {
      "enum": [
        "day",
        "month",
        "week",
        "year"
      ],
      "type": "string"
    },
    "meter": {
      "maxLength": 5000,
      "type": "string"
    },
    "usage_type": {
      "enum": [
        "licensed",
        "metered"
      ],
      "type": "string"
    }
  },
  "title": "all_prices_recurring_params",
  "type": "object"
}