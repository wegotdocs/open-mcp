{
  "type": "object",
  "properties": {
    "settlement": {
      "description": "The settlement token to mark as complete.",
      "maxLength": 5000,
      "type": "string"
    }
  },
  "required": [
    "settlement"
  ]
}