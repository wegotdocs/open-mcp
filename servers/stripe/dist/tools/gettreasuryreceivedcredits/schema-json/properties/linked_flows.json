{
  "description": "Only return ReceivedCredits described by the flow.",
  "properties": {
    "source_flow_type": {
      "enum": [
        "credit_reversal",
        "other",
        "outbound_payment",
        "outbound_transfer",
        "payout"
      ],
      "type": "string"
    }
  },
  "required": [
    "source_flow_type"
  ],
  "title": "linked_flows_param",
  "type": "object"
}