{
  "type": "object",
  "properties": {
    "source": {
      "maxLength": 5000,
      "type": "string"
    },
    "source_transaction": {
      "maxLength": 5000,
      "type": "string"
    },
    "expand": {
      "description": "Specifies which fields in the response should be expanded.",
      "items": {
        "maxLength": 5000,
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "source",
    "source_transaction"
  ]
}