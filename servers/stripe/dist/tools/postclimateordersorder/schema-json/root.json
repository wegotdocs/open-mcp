{
  "type": "object",
  "properties": {
    "order": {
      "description": "Unique identifier of the order.",
      "maxLength": 5000,
      "type": "string"
    }
  },
  "required": [
    "order"
  ]
}