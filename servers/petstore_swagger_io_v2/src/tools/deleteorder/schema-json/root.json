{
  "type": "object",
  "properties": {
    "orderId": {
      "description": "ID of the order that needs to be deleted",
      "type": "integer",
      "format": "int64",
      "minimum": 1
    }
  },
  "required": [
    "orderId"
  ]
}