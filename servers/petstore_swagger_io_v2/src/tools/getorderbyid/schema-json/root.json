{
  "type": "object",
  "properties": {
    "orderId": {
      "description": "ID of pet that needs to be fetched",
      "type": "integer",
      "format": "int64",
      "minimum": 1,
      "maximum": 10
    }
  },
  "required": [
    "orderId"
  ]
}