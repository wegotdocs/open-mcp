{
  "type": "object",
  "properties": {
    "pagination[cursor]": {
      "description": "Identifier of the next item to fetch",
      "anyOf": [
        {
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "examples": [
        "MTAw"
      ],
      "title": "Pagination[Cursor]"
    },
    "pagination[limit]": {
      "description": "Maximum number of items to fetch",
      "default": 100,
      "examples": [
        100
      ],
      "maximum": 500,
      "minimum": 1,
      "title": "Pagination[Limit]",
      "type": "integer"
    },
    "date[ge]": {
      "description": "Filter on time entries date (greater or equal)",
      "anyOf": [
        {
          "format": "date",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "examples": [
        "2021-01-01"
      ],
      "title": "Date[Ge]"
    },
    "date[le]": {
      "description": "Filter on time entries date (less or equal)",
      "anyOf": [
        {
          "format": "date",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "examples": [
        "2021-01-31"
      ],
      "title": "Date[Le]"
    },
    "user.napta_id[eq]": {
      "description": "Filter on Napta identifier of the User",
      "anyOf": [
        {
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "examples": [
        1
      ],
      "title": "User.Napta Id[Eq]"
    },
    "user.napta_id[in]": {
      "description": "Filter on multiple Napta identifier of the Users, comma separated",
      "anyOf": [
        {
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "examples": [
        "1,7"
      ],
      "title": "User.Napta Id[In]"
    },
    "user.external_id[eq]": {
      "description": "Filter on unique reference of the User (see [External Ids](#tag/External-Ids)).",
      "anyOf": [
        {
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "examples": [
        "agent-007"
      ],
      "title": "User.External Id[Eq]"
    },
    "user.external_id[in]": {
      "description": "Filter on multiple unique reference of the Users, comma separated (see [External Ids](#tag/External-Ids)).",
      "anyOf": [
        {
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "examples": [
        "agent-007,agent-q"
      ],
      "title": "User.External Id[In]"
    },
    "workload_unit": {
      "description": "Expected unit of workload in the response",
      "default": "days",
      "enum": [
        "days",
        "hours"
      ],
      "examples": [
        "days"
      ],
      "title": "Workload Unit",
      "type": "string"
    }
  },
  "required": []
}