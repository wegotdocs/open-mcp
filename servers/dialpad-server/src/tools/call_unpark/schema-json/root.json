{
  "type": "object",
  "properties": {
    "id": {
      "description": "The call's id.",
      "format": "int64",
      "type": "integer"
    },
    "user_id": {
      "description": "The id of the user who should unpark the call.",
      "format": "int64",
      "nullable": true,
      "type": "integer"
    }
  },
  "required": [
    "id",
    "user_id"
  ]
}