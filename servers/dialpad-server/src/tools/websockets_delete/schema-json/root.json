{
  "type": "object",
  "properties": {
    "id": {
      "description": "The websocket's ID, which is generated after creating a websocket successfully.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "id"
  ]
}