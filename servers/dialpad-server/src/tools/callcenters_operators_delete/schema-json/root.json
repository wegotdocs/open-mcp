{
  "type": "object",
  "properties": {
    "id": {
      "description": "The call center's id.",
      "format": "int64",
      "type": "integer"
    },
    "user_id": {
      "description": "ID of the operator to remove.",
      "format": "int64",
      "nullable": true,
      "type": "integer"
    }
  },
  "required": [
    "id",
    "user_id"
  ]
}