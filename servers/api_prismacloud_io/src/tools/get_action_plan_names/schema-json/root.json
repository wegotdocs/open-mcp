{
  "type": "object",
  "properties": {
    "x-redlock-auth": {
      "description": "Authorize using Authentication token",
      "type": "string"
    }
  },
  "required": [
    "x-redlock-auth"
  ]
}