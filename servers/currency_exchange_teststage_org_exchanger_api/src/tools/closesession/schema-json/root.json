{
  "type": "object",
  "properties": {
    "sessionId": {
      "description": "Identifier of the WebSocket session to close.",
      "type": "string"
    }
  },
  "required": [
    "sessionId"
  ]
}