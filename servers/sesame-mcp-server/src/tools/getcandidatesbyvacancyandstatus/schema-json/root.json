{
  "type": "object",
  "properties": {
    "vacancyId": {
      "description": "Vacancy ID",
      "type": "string",
      "format": "uuid"
    },
    "statusId": {
      "description": "Status ID optional",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "vacancyId"
  ]
}