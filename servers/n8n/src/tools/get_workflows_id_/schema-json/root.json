{
  "type": "object",
  "properties": {
    "id": {
      "description": "The ID of the workflow.",
      "type": "string"
    },
    "excludePinnedData": {
      "description": "Set this to avoid retrieving pinned data",
      "type": "boolean",
      "example": true
    }
  },
  "required": [
    "id"
  ]
}