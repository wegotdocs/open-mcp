{
  "type": "object",
  "properties": {
    "name": {
      "description": "The name for the `Knowledge Base`",
      "type": "string",
      "maxLength": 256,
      "example": "Totoy Customer Support Assistant"
    },
    "instructions": {
      "description": "The instructions for the `Knowledge Base`.",
      "type": "string",
      "maxLength": 4096,
      "example": "You are a customer support assistant for the customers of the company Totoy."
    },
    "project_id": {
      "description": "The unique identifier for the `Project` that the `Knowledge Base` will be assigned to. If no `project_id` is provided, the `Knowledge Base` will be assigned to the default `Project`.",
      "maxLength": 28,
      "pattern": "^pj_[a-zA-Z0-9]{25}$",
      "type": "string",
      "example": "pj_avX7imfLaPcQnv5ckvGlOEBA9"
    }
  },
  "required": []
}