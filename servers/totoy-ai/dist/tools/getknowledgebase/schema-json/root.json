{
  "type": "object",
  "properties": {
    "knowledge_base_id": {
      "description": "Unique identifier for a `Knowledge Base`.",
      "type": "string",
      "maxLength": 28,
      "pattern": "^kb_[a-zA-Z0-9]{25}$"
    }
  },
  "required": [
    "knowledge_base_id"
  ]
}