{
  "type": "object",
  "properties": {
    "source_id": {
      "description": "Unique identifier for a `Source` that was uploaded to Totoy.",
      "type": "string",
      "maxLength": 29,
      "pattern": "^src_[a-zA-Z0-9]{25}$"
    }
  },
  "required": [
    "source_id"
  ]
}