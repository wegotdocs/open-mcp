{
  "type": "object",
  "properties": {
    "phase_id": {
      "description": "The phase's ID",
      "type": "integer"
    },
    "expand": {
      "description": "Comma-delimited values expands the response with additional fields: `phase_tasks` - A list of the project tasks available in this phase `phase_team` - A list of assignees on this phase by `people_id` and `hourly_rate`, the list may be empty`",
      "type": "string"
    }
  },
  "required": [
    "phase_id"
  ]
}