{
  "type": "object",
  "description": "不同条件之间是 `且` 的关系",
  "properties": {
    "career": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "example": [
        "artist",
        "director"
      ],
      "description": "职业，可以多次出现。多值之间为 `且` 关系。"
    }
  }
}