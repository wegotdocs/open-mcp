{
  "type": "object",
  "properties": {
    "subject_id": {
      "description": "条目 ID",
      "title": "Subject ID",
      "minimum": 1,
      "type": "integer"
    },
    "limit": {
      "description": "分页参数",
      "title": "Limit",
      "maximum": 50,
      "minimum": 1,
      "type": "integer",
      "default": 30
    },
    "offset": {
      "description": "分页参数",
      "title": "Offset",
      "minimum": 0,
      "type": "integer",
      "default": 0
    }
  },
  "required": [
    "subject_id"
  ]
}