{
  "type": "object",
  "properties": {
    "mode": {
      "description": "Approval action to perform",
      "type": "string",
      "enum": [
        "APPROVE",
        "REJECT"
      ]
    },
    "devices": {
      "type": "array",
      "items": {
        "type": "integer",
        "format": "int32"
      }
    }
  },
  "required": [
    "mode"
  ]
}