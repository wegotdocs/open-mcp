{
  "type": "object",
  "properties": {
    "df": {
      "description": "Device filter",
      "type": "string"
    },
    "cursor": {
      "description": "Cursor name",
      "type": "string"
    },
    "pageSize": {
      "description": "Limit number of records per page",
      "maximum": 10000,
      "minimum": 10,
      "type": "integer",
      "format": "int32",
      "default": 1000
    }
  },
  "required": []
}