{
  "type": "object",
  "properties": {
    "id": {
      "description": "Unique identifier for Prompt.",
      "type": "string",
      "title": "Prompt ID"
    },
    "version_id": {
      "description": "A specific Version ID of the Prompt to retrieve to populate the template.",
      "type": "string",
      "title": "Version ID"
    },
    "environment": {
      "description": "Name of the Environment to retrieve a deployed Version from to populate the template.",
      "type": "string",
      "title": "Environment"
    }
  },
  "required": [
    "id"
  ]
}