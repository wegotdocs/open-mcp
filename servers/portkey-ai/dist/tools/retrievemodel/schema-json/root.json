{
  "type": "object",
  "properties": {
    "model": {
      "description": "The ID of the model to use for this request",
      "type": "string",
      "example": "gpt-3.5-turbo"
    }
  },
  "required": [
    "model"
  ]
}