{
  "type": "object",
  "properties": {
    "labelId": {
      "description": "ID of the label to delete",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "labelId"
  ]
}