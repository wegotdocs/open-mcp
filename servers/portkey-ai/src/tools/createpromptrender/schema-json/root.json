{
  "type": "object",
  "properties": {
    "promptId": {
      "description": "The unique identifier of the prompt template to render",
      "type": "string"
    }
  },
  "required": [
    "promptId"
  ]
}