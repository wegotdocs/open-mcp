{
  "type": "object",
  "properties": {
    "organizationId": {
      "description": "ID of the requested organization.",
      "type": "string",
      "format": "uuid"
    },
    "keyId": {
      "description": "ID of the requested key.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "organizationId",
    "keyId"
  ]
}