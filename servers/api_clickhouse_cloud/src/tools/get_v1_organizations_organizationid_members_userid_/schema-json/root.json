{
  "type": "object",
  "properties": {
    "organizationId": {
      "description": "ID of the organization the member is part of.",
      "type": "string",
      "format": "uuid"
    },
    "userId": {
      "description": "ID of the requested user.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "organizationId",
    "userId"
  ]
}