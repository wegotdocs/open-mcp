{
  "type": "object",
  "properties": {
    "organizationId": {
      "description": "ID of the requested organization.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "organizationId"
  ]
}