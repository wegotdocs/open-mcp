{
  "type": "object",
  "properties": {
    "organizationId": {
      "description": "ID of the organization that owns the service.",
      "type": "string",
      "format": "uuid"
    },
    "serviceId": {
      "description": "ID of the service to delete.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "organizationId",
    "serviceId"
  ]
}