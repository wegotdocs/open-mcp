{
  "type": "object",
  "properties": {
    "organizationId": {
      "description": "ID of the requested organization.",
      "type": "string",
      "format": "uuid"
    },
    "serviceId": {
      "description": "ID of the requested service.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "organizationId",
    "serviceId"
  ]
}