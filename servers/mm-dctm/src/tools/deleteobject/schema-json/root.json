{
  "type": "object",
  "properties": {
    "repositoryName": {
      "description": "The repository name.",
      "type": "string"
    },
    "id": {
      "description": "The r_object_id of queried object.",
      "type": "string"
    },
    "del-version": {
      "description": "Delete options for multi-version document object. * 'selected_version', delete selected version only. * 'non_used_versions', delete non used versions. * 'all_versions', delete all versions.",
      "type": "integer",
      "enum": [
        "SELECTED",
        "UNUSED",
        "ALL"
      ]
    },
    "del-non-empty": {
      "description": "Ensures whether to delete the folder instance when it is not empty. * 'true', would delete the folder and all of its descendants. * 'false', would not delete the folder instance only.",
      "type": "string"
    },
    "del-all-links": {
      "description": "Ensures whether to unlink the links on folder instance and all its descendants when deleted. * 'true', would delete the folder and all descendants, regardless of other folders into which they are linked * 'false', would unlink folder descendants from the folder being deleted if the folder descendants have links to other folders, and to remove folder descendants that are only linked to the folder being deleted.",
      "type": "string"
    },
    "del-vd-all": {
      "description": "Whether remove the all the descendants if the object is virtual.",
      "type": "string",
      "default": false
    }
  },
  "required": [
    "repositoryName",
    "id"
  ]
}