{
  "type": "object",
  "properties": {
    "resourceId": {
      "description": "The resource ID to scope the access request to. Must be a valid ID for the resource type.",
      "type": "string"
    }
  },
  "required": [
    "resourceId"
  ]
}