import { z } from "zod"

export const inputParamsSchema = {
  "editorSuggestions": z.array(z.enum(["videoAutoLevels","videoStabilize","videoCrop","audioQuietAudioSwap"])).describe("A list of video editing operations that might improve the video quality or playback experience of the uploaded video.").optional(),
  "processingErrors": z.array(z.enum(["audioFile","imageFile","projectFile","notAVideoFile","docFile","archiveFile","unsupportedSpatialAudioLayout"])).describe("A list of errors that will prevent YouTube from successfully processing the uploaded video video. These errors indicate that, regardless of the video's current processing status, eventually, that status will almost certainly be failed.").optional(),
  "processingHints": z.array(z.enum(["nonStreamableMov","sendBestQualityVideo","sphericalVideo","spatialAudio","vrVideo","hdrVideo"])).describe("A list of suggestions that may improve YouTube's ability to process the video.").optional(),
  "processingWarnings": z.array(z.enum(["unknownContainer","unknownVideoCodec","unknownAudioCodec","inconsistentResolution","hasEditlist","problematicVideoCodec","problematicAudioCodec","unsupportedVrStereoMode","unsupportedSphericalProjectionType","unsupportedHdrPixelFormat","unsupportedHdrColorMetadata","problematicHdrLookupTable"])).describe("A list of reasons why YouTube may have difficulty transcoding the uploaded video or that might result in an erroneous transcoding. These warnings are generated before YouTube actually processes the uploaded video file. In addition, they identify issues that are unlikely to cause the video processing to fail but that might cause problems such as sync issues, video artifacts, or a missing audio track.").optional(),
  "tagSuggestions": z.array(z.object({ "categoryRestricts": z.array(z.string()).describe("A set of video categories for which the tag is relevant. You can use this information to display appropriate tag suggestions based on the video category that the video uploader associates with the video. By default, tag suggestions are relevant for all categories if there are no restricts defined for the keyword.").optional(), "tag": z.string().describe("The keyword tag suggested for the video.").optional() }).describe("A single tag suggestion with it's relevance information.")).describe("A list of keyword tags that could be added to the video's metadata to increase the likelihood that users will locate your video when searching or browsing on YouTube.").optional()
}