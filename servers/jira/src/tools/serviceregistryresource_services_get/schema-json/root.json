{
  "type": "object",
  "properties": {
    "serviceIds": {
      "description": "The ID of the services (the strings starting with \"b:\" need to be decoded in Base64).",
      "items": {
        "type": "string"
      },
      "maxItems": 20,
      "minItems": 1,
      "type": "array"
    }
  },
  "required": [
    "serviceIds"
  ]
}