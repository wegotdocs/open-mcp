{
  "type": "object",
  "properties": {
    "fieldId": {
      "description": "The ID of the custom field.",
      "type": "string"
    },
    "contextId": {
      "description": "The ID of the context.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "fieldId",
    "contextId"
  ]
}