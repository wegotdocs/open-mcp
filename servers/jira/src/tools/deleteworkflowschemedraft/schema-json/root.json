{
  "type": "object",
  "properties": {
    "id": {
      "description": "The ID of the active workflow scheme that the draft was created from.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "id"
  ]
}