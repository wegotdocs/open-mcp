{
  "type": "object",
  "properties": {
    "projectId": {
      "description": "The ID of the project.",
      "format": "int64",
      "type": "integer"
    },
    "level": {
      "description": "The level of the issue type to filter by. Use:\n\n *  `-1` for Subtask.\n *  `0` for Base.\n *  `1` for Epic.",
      "format": "int32",
      "type": "integer"
    }
  },
  "required": [
    "projectId"
  ]
}