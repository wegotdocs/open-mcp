{
  "type": "object",
  "properties": {
    "issueIdOrKey": {
      "description": "The ID or key of the issue.",
      "type": "string"
    },
    "worklogId": {
      "description": "The ID of the worklog.",
      "type": "string"
    }
  },
  "required": [
    "issueIdOrKey",
    "worklogId"
  ]
}