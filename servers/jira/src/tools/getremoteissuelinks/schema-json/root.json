{
  "type": "object",
  "properties": {
    "issueIdOrKey": {
      "description": "The ID or key of the issue.",
      "example": "10000",
      "type": "string"
    },
    "globalId": {
      "description": "The global ID of the remote issue link.",
      "type": "string"
    }
  },
  "required": [
    "issueIdOrKey"
  ]
}