{
  "type": "object",
  "properties": {
    "issueIdOrKey": {
      "description": "The ID or key of the issue.",
      "example": "10000",
      "type": "string"
    },
    "linkId": {
      "description": "The ID of a remote issue link.",
      "example": "10000",
      "type": "string"
    }
  },
  "required": [
    "issueIdOrKey",
    "linkId"
  ]
}