{
  "type": "object",
  "properties": {
    "projectIdOrKey": {
      "description": "The ID or (case-sensitive) key of the project.",
      "type": "string"
    }
  },
  "required": [
    "projectIdOrKey"
  ]
}