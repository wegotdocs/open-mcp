{
  "type": "object",
  "properties": {
    "issueIdOrKey": {
      "description": "The ID or key of the issue.",
      "type": "string"
    },
    "linkId": {
      "description": "The ID of the remote issue link.",
      "type": "string"
    }
  },
  "required": [
    "issueIdOrKey",
    "linkId"
  ]
}