{
  "type": "object",
  "properties": {
    "projectIdOrKey": {
      "description": "The project ID or (case-sensitive) key.",
      "type": "string"
    },
    "id": {
      "description": "The ID of the avatar.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "projectIdOrKey",
    "id"
  ]
}