{
  "type": "object",
  "properties": {
    "id": {
      "description": "The ID of the active workflow scheme that the draft is created from.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "id"
  ]
}