{
  "type": "object",
  "properties": {
    "type": {
      "description": "The avatar type.",
      "enum": [
        "issuetype",
        "project",
        "user",
        "priority"
      ],
      "example": "project",
      "type": "string"
    }
  },
  "required": [
    "type"
  ]
}