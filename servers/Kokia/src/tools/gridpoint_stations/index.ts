import { inputParamsSchema } from "./schema/root.js"
import type { OpenMCPServerTool } from "@open-mcp/core"

const tool: OpenMCPServerTool = {
  "toolName": "gridpoint_stations",
  "toolDescription": "Returns a list of observation stations usable for a given 2.5km grid area",
  "baseUrl": "https://api.weather.gov/v1",
  "path": "/gridpoints/{wfo}/{x},{y}/stations",
  "method": "get",
  "security": [
    {
      "key": "User-Agent",
      "value": "<mcp-env-var>USER_AGENT</mcp-env-var>",
      "in": "header",
      "envVarName": "USER_AGENT"
    }
  ],
  "paramsMap": {},
  inputParamsSchema
}

export default tool