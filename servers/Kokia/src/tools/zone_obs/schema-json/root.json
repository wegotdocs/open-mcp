{
  "type": "object",
  "properties": {
    "start": {
      "description": "Start date/time",
      "type": "string",
      "format": "date-time"
    },
    "end": {
      "description": "End date/time",
      "type": "string",
      "format": "date-time"
    },
    "limit": {
      "description": "Limit",
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
    }
  },
  "required": []
}