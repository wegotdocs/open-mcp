{
  "type": "object",
  "properties": {
    "token": {
      "description": "Takes in the token from a previous call to return a result.",
      "type": "string"
    }
  },
  "required": [
    "token"
  ]
}