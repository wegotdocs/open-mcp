{
  "type": "object",
  "properties": {
    "id": {
      "description": "The object’s conceptual ID, which is synthesized from the process and Runtime IDs.",
      "type": "string"
    }
  },
  "required": [
    "id"
  ]
}