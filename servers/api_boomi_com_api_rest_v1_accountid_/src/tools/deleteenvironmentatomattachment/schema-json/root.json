{
  "type": "object",
  "properties": {
    "id": {
      "description": "The object’s conceptual ID, which is synthesized from the Runtime and environment IDs.",
      "type": "string"
    }
  },
  "required": [
    "id"
  ]
}