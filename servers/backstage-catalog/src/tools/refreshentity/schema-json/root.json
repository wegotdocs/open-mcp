{
  "type": "object",
  "properties": {
    "authorizationToken": {
      "type": "string"
    },
    "entityRef": {
      "type": "string",
      "description": "The reference to a single entity that should be refreshed"
    }
  },
  "required": [
    "entityRef"
  ]
}