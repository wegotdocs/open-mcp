{
  "type": "object",
  "properties": {
    "type": {
      "description": "Restrict results to a certain type of 1-Click.",
      "type": "string",
      "enum": [
        "droplet",
        "kubernetes"
      ]
    }
  },
  "required": []
}