{
  "type": "object",
  "properties": {
    "droplet_id": {
      "description": "A unique identifier for a Droplet instance.",
      "type": "integer",
      "minimum": 1
    }
  },
  "required": [
    "droplet_id"
  ]
}