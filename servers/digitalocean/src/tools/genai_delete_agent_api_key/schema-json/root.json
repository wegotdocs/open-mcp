{
  "type": "object",
  "properties": {
    "agent_uuid": {
      "description": "A unique identifier for your agent.",
      "type": "string"
    },
    "api_key_uuid": {
      "description": "API key for an agent.",
      "type": "string"
    }
  },
  "required": [
    "agent_uuid",
    "api_key_uuid"
  ]
}