{
  "type": "object",
  "properties": {
    "certificate_id": {
      "description": "A unique identifier for a certificate.",
      "type": "string",
      "format": "uuid",
      "minimum": 1
    }
  },
  "required": [
    "certificate_id"
  ]
}