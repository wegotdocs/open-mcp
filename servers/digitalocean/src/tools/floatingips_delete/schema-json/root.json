{
  "type": "object",
  "properties": {
    "floating_ip": {
      "description": "A floating IP address.",
      "type": "string",
      "format": "ipv4",
      "minimum": 1
    }
  },
  "required": [
    "floating_ip"
  ]
}