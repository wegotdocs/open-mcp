{
  "type": "object",
  "properties": {
    "database_cluster_uuid": {
      "description": "A unique identifier for a database cluster.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "database_cluster_uuid"
  ]
}