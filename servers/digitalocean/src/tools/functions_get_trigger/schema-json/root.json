{
  "type": "object",
  "properties": {
    "namespace_id": {
      "description": "The ID of the namespace to be managed.",
      "type": "string"
    },
    "trigger_name": {
      "description": "The name of the trigger to be managed.",
      "type": "string"
    }
  },
  "required": [
    "namespace_id",
    "trigger_name"
  ]
}