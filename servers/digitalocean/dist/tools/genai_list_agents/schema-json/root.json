{
  "type": "object",
  "properties": {
    "only_deployed": {
      "description": "Only list agents that are deployed.",
      "type": "boolean"
    },
    "page": {
      "description": "Page number.",
      "type": "integer"
    },
    "per_page": {
      "description": "Items per page.",
      "type": "integer"
    }
  },
  "required": []
}