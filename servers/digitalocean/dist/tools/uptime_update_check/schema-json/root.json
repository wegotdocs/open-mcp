{
  "type": "object",
  "properties": {
    "check_id": {
      "description": "A unique identifier for a check.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "check_id"
  ]
}