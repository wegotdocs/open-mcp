{
  "type": "object",
  "properties": {
    "volume_id": {
      "description": "The ID of the block storage volume.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "volume_id"
  ]
}