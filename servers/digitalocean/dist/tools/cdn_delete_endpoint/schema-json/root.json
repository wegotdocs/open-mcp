{
  "type": "object",
  "properties": {
    "cdn_id": {
      "description": "A unique identifier for a CDN endpoint.",
      "type": "string",
      "format": "uuid",
      "minimum": 1
    }
  },
  "required": [
    "cdn_id"
  ]
}