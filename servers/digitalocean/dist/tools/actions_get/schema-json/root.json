{
  "type": "object",
  "properties": {
    "action_id": {
      "description": "A unique numeric ID that can be used to identify and reference an action.",
      "type": "integer",
      "minimum": 1
    }
  },
  "required": [
    "action_id"
  ]
}