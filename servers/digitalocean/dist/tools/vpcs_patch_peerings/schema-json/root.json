{
  "type": "object",
  "properties": {
    "vpc_id": {
      "description": "A unique identifier for a VPC.",
      "type": "string",
      "format": "uuid",
      "minimum": 1
    },
    "vpc_peering_id": {
      "description": "A unique identifier for a VPC peering.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "vpc_id",
    "vpc_peering_id"
  ]
}