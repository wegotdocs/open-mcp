{
  "type": "object",
  "properties": {
    "vpc_id": {
      "description": "A unique identifier for a VPC.",
      "type": "string",
      "format": "uuid",
      "minimum": 1
    }
  },
  "required": [
    "vpc_id"
  ]
}