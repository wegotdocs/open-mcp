{
  "description": "Optional data to be sent to function while triggering the function.",
  "type": "object",
  "nullable": true,
  "properties": {
    "name": {
      "type": "string",
      "example": "Welcome to DO!"
    }
  }
}