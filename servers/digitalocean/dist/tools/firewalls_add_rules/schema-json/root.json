{
  "type": "object",
  "properties": {
    "firewall_id": {
      "description": "A unique ID that can be used to identify and reference a firewall.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "firewall_id"
  ]
}