{
  "type": "object",
  "properties": {
    "agent_uuid": {
      "description": "A unique identifier for an agent.",
      "type": "string"
    },
    "knowledge_base_uuid": {
      "description": "A unique identifier for a knowledge base.",
      "type": "string"
    }
  },
  "required": [
    "agent_uuid",
    "knowledge_base_uuid"
  ]
}