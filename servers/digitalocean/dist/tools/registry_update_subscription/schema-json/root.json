{
  "type": "object",
  "properties": {
    "tier_slug": {
      "type": "string",
      "enum": [
        "starter",
        "basic",
        "professional"
      ],
      "example": "basic",
      "description": "The slug of the subscription tier to sign up for."
    }
  },
  "required": []
}