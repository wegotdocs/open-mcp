{
  "type": "object",
  "properties": {
    "check_id": {
      "description": "A unique identifier for a check.",
      "type": "string",
      "format": "uuid"
    },
    "alert_id": {
      "description": "A unique identifier for an alert.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "check_id",
    "alert_id"
  ]
}