{
  "type": "object",
  "properties": {
    "project_id": {
      "description": "A unique identifier for a project.",
      "type": "string",
      "format": "uuid",
      "minimum": 1
    }
  },
  "required": [
    "project_id"
  ]
}