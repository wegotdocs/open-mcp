{
  "type": "object",
  "properties": {
    "lb_id": {
      "description": "A unique identifier for a load balancer.",
      "type": "string",
      "minimum": 1
    }
  },
  "required": [
    "lb_id"
  ]
}