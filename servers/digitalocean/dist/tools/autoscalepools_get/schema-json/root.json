{
  "type": "object",
  "properties": {
    "autoscale_pool_id": {
      "description": "A unique identifier for an autoscale pool.",
      "type": "string"
    }
  },
  "required": [
    "autoscale_pool_id"
  ]
}