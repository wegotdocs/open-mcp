{
  "type": "object",
  "properties": {
    "config_id": {
      "description": "The Linode Config ID to boot into.",
      "example": "{{config_id}}",
      "nullable": true,
      "type": "integer"
    }
  },
  "required": []
}