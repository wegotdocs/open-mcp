{
  "type": "object",
  "properties": {
    "size": {
      "description": "The desired size, in MB, of the disk.",
      "example": "{{size}}",
      "minimum": 1,
      "type": "integer"
    }
  },
  "required": [
    "size"
  ]
}