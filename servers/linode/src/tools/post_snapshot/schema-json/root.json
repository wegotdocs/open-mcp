{
  "type": "object",
  "properties": {
    "label": {
      "description": "The label for the new snapshot.",
      "example": "{{label}}",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "label"
  ]
}