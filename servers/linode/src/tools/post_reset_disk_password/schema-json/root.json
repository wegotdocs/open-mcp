{
  "type": "object",
  "properties": {
    "password": {
      "description": "The new root password for the OS installed on this Disk. The password must meet the complexity strength validation requirements for a strong password.",
      "example": "{{password}}",
      "type": "string"
    }
  },
  "required": [
    "password"
  ]
}