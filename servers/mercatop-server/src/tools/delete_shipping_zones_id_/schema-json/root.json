{
  "type": "object",
  "properties": {
    "id": {
      "description": "Unique ID for the resource.",
      "type": "integer"
    },
    "force": {
      "description": "Whether to bypass trash and force deletion.",
      "type": "boolean"
    }
  },
  "required": [
    "id"
  ]
}