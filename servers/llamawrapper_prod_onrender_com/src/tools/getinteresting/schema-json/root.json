{
  "type": "object",
  "properties": {
    "time_period": {
      "description": "The time period for which to retrieve data. Can be day or week.",
      "type": "string",
      "enum": [
        "day",
        "week"
      ],
      "default": "week"
    }
  },
  "required": []
}