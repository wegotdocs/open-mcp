import { z } from "zod"

export const inputParamsSchema = {
  "expected_frequency": z.enum(["once_per_month","twice_per_month","once_per_week","daily"]).describe("The originator's expected transfer frequency."),
  "expected_highest_amount": z.string().describe("The originator’s expected highest amount for a single credit transfer."),
  "expected_average_amount": z.string().describe("The originator’s expected average amount per credit."),
  "expected_monthly_amount": z.string().describe("The originator’s monthly expected ACH credit processing amount for the next 6-12 months."),
  "sec_codes": z.array(z.enum(["ccd","ppd","web"]).describe("Specifies the use case of the transfer. Required for transfers on an ACH network.\n\n`\"ccd\"` - Corporate Credit or Debit - fund transfer between two corporate bank accounts\n\n`\"ppd\"` - Prearranged Payment or Deposit - The transfer is part of a pre-existing relationship with a consumer. Authorization was obtained in writing either in person or via an electronic document signing, e.g. Docusign, by the consumer. Can be used for credits or debits.\n\n`\"web\"` - Internet-Initiated Entry. The transfer debits a consumer’s bank account. Authorization from the consumer is obtained over the Internet (e.g. a web or mobile application). Can be used for single debits or recurring debits.")).describe("Specifies the expected use cases for the originator’s credit transfers. This should be a list that contains one or more of the following codes:\n\n`\"ccd\"` - Corporate Credit or Debit - fund transfer between two corporate bank accounts\n\n`\"ppd\"` - Prearranged Payment or Deposit - The transfer is part of a pre-existing relationship with a consumer. Authorization was obtained in writing either in person or via an electronic document signing, e.g. Docusign, by the consumer. Can be used for credits or debits.\n\n`\"web\"` - Internet-Initiated Entry. The transfer debits a consumer’s bank account. Authorization from the consumer is obtained over the Internet (e.g. a web or mobile application). Can be used for single debits or recurring debits.")
}