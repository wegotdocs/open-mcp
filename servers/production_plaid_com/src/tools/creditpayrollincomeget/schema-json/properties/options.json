{
  "description": "An optional object for `/credit/payroll_income/get` request options.",
  "type": "object",
  "properties": {
    "item_ids": {
      "type": "array",
      "description": "An array of `item_id`s whose payroll information is returned. Each `item_id` should uniquely identify a payroll income item. If this field is not provided, all `item_id`s associated with the `user_token` will returned in the response.",
      "items": {
        "type": "string"
      }
    }
  }
}