{
  "type": "object",
  "description": "Order of sorting the results in the format: {field: order}\n",
  "properties": {
    "keywords": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Total number of keywords."
    },
    "traff": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Traffic generated by the keywords."
    },
    "visible": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Visibility score."
    },
    "visible_static": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Static visibility."
    },
    "new_keywords": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Number of newly discovered keywords."
    },
    "out_keywords": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Number of keywords that dropped out."
    },
    "rised_keywords": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Number of keywords that increased in rank."
    },
    "down_keywords": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Number of keywords that decreased in rank."
    },
    "ad_keywords": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Number of keywords used in paid ads."
    },
    "ads": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Number of ads."
    },
    "date": {
      "type": "string",
      "enum": [
        "asc",
        "desc"
      ],
      "description": "Date of the data entry."
    }
  }
}