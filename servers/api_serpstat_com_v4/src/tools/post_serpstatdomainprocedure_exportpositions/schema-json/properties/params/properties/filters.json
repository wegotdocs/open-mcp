{
  "type": "object",
  "description": "Filters for search. Fields are combined using the **AND** logic. Numeric range fields (`_from` and `_to`) allow specifying minimum and maximum values. List fields (`_contain` and `_not_contain`) specify inclusion or exclusion criteria",
  "properties": {
    "right_spelling": {
      "type": "boolean",
      "description": "Filters by spelling correction. If true, results will include corrected spelling; if false, only exact spelling is used."
    },
    "position_dynamic": {
      "type": "string",
      "enum": [
        "new",
        "rise",
        "down"
      ],
      "description": "Filters results by position dynamics:\n- 'new' for newly appearing results,\n- 'rise' for results that have moved up,\n- 'down' for results that have moved down."
    }
  }
}