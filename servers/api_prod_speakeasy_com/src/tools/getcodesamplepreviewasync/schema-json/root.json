{
  "type": "object",
  "properties": {
    "jobID": {
      "description": "The ID of the job to check the status and retrieve results",
      "type": "string"
    }
  },
  "required": [
    "jobID"
  ]
}