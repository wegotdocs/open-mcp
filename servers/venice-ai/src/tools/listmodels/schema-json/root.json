{
  "type": "object",
  "properties": {
    "type": {
      "description": "Filter models by type. Use \"all\" to get all model types.",
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "embedding",
            "image",
            "text",
            "tts",
            "upscale"
          ]
        },
        {
          "type": "string",
          "enum": [
            "all",
            "code"
          ]
        }
      ],
      "example": "text"
    }
  },
  "required": []
}