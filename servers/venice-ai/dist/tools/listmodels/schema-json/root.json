{
  "type": "object",
  "properties": {
    "type": {
      "description": "Filter models by type. Use \"all\" to get all model types.",
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "image",
            "text",
            "embedding",
            "tts"
          ]
        },
        {
          "type": "string",
          "enum": [
            "all",
            "code"
          ]
        }
      ],
      "example": "text"
    }
  },
  "required": []
}