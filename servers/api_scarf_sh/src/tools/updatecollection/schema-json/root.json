{
  "type": "object",
  "properties": {
    "pattern": {
      "type": "string",
      "example": "pizza-joes/*"
    },
    "public_domain": {
      "type": "string",
      "example": "cr.pizza-place-locator.tiki"
    },
    "backend_domain": {
      "type": "string",
      "example": "docker.io"
    }
  },
  "required": [
    "pattern",
    "backend_domain"
  ]
}