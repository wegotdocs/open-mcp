{
  "type": "object",
  "properties": {
    "project_id": {
      "type": "integer",
      "description": "ID del progetto nel database",
      "examples": [
        2392
      ]
    },
    "command": {
      "type": "string",
      "description": "Comando SSH da eseguire sul server remoto",
      "examples": [
        "df -h"
      ]
    }
  },
  "required": [
    "project_id",
    "command"
  ]
}