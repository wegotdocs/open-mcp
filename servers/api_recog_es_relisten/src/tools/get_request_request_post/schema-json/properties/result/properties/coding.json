{
  "additionalProperties": true,
  "type": "object",
  "title": "Coding",
  "description": "The coding information of the consultation. This dictionary follows the same schema as in `answers` with the coding that applies to each field, if any.",
  "default": {},
  "properties": {}
}