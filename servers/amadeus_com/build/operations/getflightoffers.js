import { z } from "zod";
export const toolName = `getflightoffers`;
export const toolDescription = `Return list of Flight Offers based on searching criteria.`;
export const baseUrl = `https://test.api.amadeus.com/v2`;
export const path = `/shopping/flight-offers`;
export const method = `get`;
export const security = [];
export const inputParams = z.object({ "query": z.object({ "originLocationCode": z.string().regex(new RegExp("[A-Z]{3}")).describe("city/airport [IATA code](http://www.iata.org/publications/Pages/code-search.aspx) from which the traveler will depart, e.g. BOS for Boston"), "destinationLocationCode": z.string().regex(new RegExp("[A-Z]{3}")).describe("city/airport [IATA code](http://www.iata.org/publications/Pages/code-search.aspx) to which the traveler is going, e.g. PAR for Paris"), "departureDate": z.string().date().describe("the date on which the traveler will depart from the origin to go to the destination. Dates are specified in the [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) YYYY-MM-DD format, e.g. 2017-12-25"), "returnDate": z.string().date().describe("the date on which the traveler will depart from the destination to return to the origin. If this parameter is not specified, only one-way itineraries are found. If this parameter is specified, only round-trip itineraries are found. Dates are specified in the [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) YYYY-MM-DD format, e.g. 2018-02-28").optional(), "adults": z.number().int().gte(1).lte(9).describe("the number of adult travelers (age 12 or older on date of departure)."), "children": z.number().int().gte(0).lte(9).describe("the number of child travelers (older than age 2 and younger than age 12 on date of departure) who will each have their own separate seat. If specified, this number should be greater than or equal to 0").optional(), "infants": z.number().int().gte(0).lte(9).describe("the number of infant travelers (whose age is less or equal to 2 on date of departure). Infants travel on the lap of an adult traveler, and thus the number of infants must not exceed the number of adults. If specified, this number should be greater than or equal to 0").optional(), "travelClass": z.enum(["ECONOMY", "PREMIUM_ECONOMY", "BUSINESS", "FIRST"]).describe("most of the flight time should be spent in a cabin of this quality or higher. The accepted travel class is economy, premium economy, business or first class. If no travel class is specified, the search considers any travel class").optional(), "includedAirlineCodes": z.string().regex(new RegExp("[0-9A-Z]{2}")).describe("This option ensures that the system will only consider these airlines. This can not be cumulated with parameter excludedAirlineCodes.\n\nAirlines are specified as [IATA airline codes](http://www.iata.org/publications/Pages/code-search.aspx) and are comma-separated, e.g. 6X,7X,8X\n").optional(), "excludedAirlineCodes": z.string().regex(new RegExp("[0-9A-Z]{2}")).describe("This option ensures that the system will ignore these airlines. This can not be cumulated with parameter includedAirlineCodes.\n\nAirlines are specified as [IATA airline codes](http://www.iata.org/publications/Pages/code-search.aspx) and are comma-separated, e.g. 6X,7X,8X\n").optional(), "nonStop": z.boolean().describe("if set to true, the search will find only flights going from the origin to the destination with no stop in between"), "currencyCode": z.string().regex(new RegExp("[A-Z]{3}")).describe("the preferred currency for the flight offers. Currency is specified in the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) format, e.g. EUR for Euro").optional(), "maxPrice": z.number().int().gte(1).describe("maximum price per traveler. By default, no limit is applied. If specified, the value should be a positive number with no decimals").optional(), "max": z.number().int().gte(1).describe("maximum number of flight offers to return. If specified, the value should be greater than or equal to 1") }).optional() }).shape;
