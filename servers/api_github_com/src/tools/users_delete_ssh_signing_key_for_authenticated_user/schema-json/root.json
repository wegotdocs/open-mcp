{
  "type": "object",
  "properties": {
    "ssh_signing_key_id": {
      "description": "The unique identifier of the SSH signing key.",
      "type": "integer"
    }
  },
  "required": [
    "ssh_signing_key_id"
  ]
}