import { inputParamsSchema } from "./schema/root.js"
import type { OpenMCPServerTool } from "@open-mcp/core"

const tool: OpenMCPServerTool = {
  "toolName": "users_list_emails_for_authenticated_user",
  "toolDescription": "List email addresses for the authenticated user",
  "baseUrl": "https://api.github.com",
  "path": "/user/emails",
  "method": "get",
  "security": [],
  "paramsMap": {
    "query": {
      "per_page": "per_page",
      "page": "page"
    }
  },
  inputParamsSchema
}

export default tool