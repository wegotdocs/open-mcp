import { z } from "zod"

export const inputParamsSchema = {
  "title": z.string().describe("The title of the check run."),
  "summary": z.string().max(65535).describe("The summary of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters."),
  "text": z.string().max(65535).describe("The details of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters.").optional(),
  "annotations": z.array(z.object({ "path": z.string().describe("The path of the file to add an annotation to. For example, `assets/css/main.css`."), "start_line": z.number().int().describe("The start line of the annotation. Line numbers start at 1."), "end_line": z.number().int().describe("The end line of the annotation."), "start_column": z.number().int().describe("The start column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values. Column numbers start at 1.").optional(), "end_column": z.number().int().describe("The end column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values.").optional(), "annotation_level": z.enum(["notice","warning","failure"]).describe("The level of the annotation."), "message": z.string().describe("A short description of the feedback for these lines of code. The maximum size is 64 KB."), "title": z.string().describe("The title that represents the annotation. The maximum size is 255 characters.").optional(), "raw_details": z.string().describe("Details about this annotation. The maximum size is 64 KB.").optional() })).max(50).describe("Adds information from your analysis to specific lines of code. Annotations are visible on GitHub in the **Checks** and **Files changed** tab of the pull request. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about how you can view annotations on GitHub, see \"[About status checks](https://docs.github.com/articles/about-status-checks#checks)\".").optional(),
  "images": z.array(z.object({ "alt": z.string().describe("The alternative text for the image."), "image_url": z.string().describe("The full URL of the image."), "caption": z.string().describe("A short image description.").optional() })).describe("Adds images to the output displayed in the GitHub pull request UI.").optional()
}