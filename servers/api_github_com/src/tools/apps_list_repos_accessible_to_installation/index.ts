import { inputParamsSchema } from "./schema/root.js"
import type { OpenMCPServerTool } from "@open-mcp/core"

const tool: OpenMCPServerTool = {
  "toolName": "apps_list_repos_accessible_to_installation",
  "toolDescription": "List repositories accessible to the app installation",
  "baseUrl": "https://api.github.com",
  "path": "/installation/repositories",
  "method": "get",
  "security": [],
  "paramsMap": {
    "query": {
      "per_page": "per_page",
      "page": "page"
    }
  },
  inputParamsSchema
}

export default tool