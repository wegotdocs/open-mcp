{
  "type": "object",
  "properties": {
    "apiKey": {
      "description": "Your API key",
      "type": "string"
    },
    "url": {
      "description": "Define an article URL, that'll be used to extract the article information.",
      "type": "string"
    }
  },
  "required": [
    "apiKey",
    "url"
  ]
}