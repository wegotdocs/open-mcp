{
  "type": "object",
  "properties": {
    "apiKey": {
      "description": "Your API key",
      "type": "string"
    },
    "text": {
      "description": "Define a text that'll be used to detect the language in which it's written.",
      "type": "string"
    }
  },
  "required": [
    "apiKey",
    "text"
  ]
}