{
  "type": "object",
  "properties": {
    "apiKey": {
      "description": "Your API key",
      "type": "string"
    },
    "prefix": {
      "description": "Determine the search condition.",
      "type": "string"
    }
  },
  "required": [
    "apiKey"
  ]
}