{
  "type": "object",
  "description": "Should the configured AML pre-screening policy be enabled or not",
  "properties": {
    "enabled": {
      "type": "boolean"
    }
  },
  "required": [
    "enabled"
  ]
}