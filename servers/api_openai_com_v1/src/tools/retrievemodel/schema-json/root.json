{
  "type": "object",
  "properties": {
    "model": {
      "description": "The ID of the model to use for this request",
      "type": "string",
      "example": "gpt-4o-mini"
    }
  },
  "required": [
    "model"
  ]
}