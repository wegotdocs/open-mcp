{
  "type": "object",
  "properties": {
    "project_id": {
      "description": "The ID of the project.",
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "The name of the service account being created."
    }
  },
  "required": [
    "project_id",
    "name"
  ]
}