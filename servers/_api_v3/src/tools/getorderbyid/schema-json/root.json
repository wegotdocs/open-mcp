{
  "type": "object",
  "properties": {
    "orderId": {
      "description": "ID of order that needs to be fetched",
      "type": "integer",
      "format": "int64"
    }
  },
  "required": [
    "orderId"
  ]
}