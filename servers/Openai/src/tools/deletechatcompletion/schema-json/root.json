{
  "type": "object",
  "properties": {
    "completion_id": {
      "description": "The ID of the chat completion to delete.",
      "type": "string"
    }
  },
  "required": [
    "completion_id"
  ]
}