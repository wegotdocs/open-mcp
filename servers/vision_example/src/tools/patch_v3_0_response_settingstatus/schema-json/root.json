{
  "type": "object",
  "properties": {
    "endpointActionException": {
      "description": "The status of the endpoint response action exclusion setting.",
      "type": "string",
      "enum": [
        "enabled",
        "disabled"
      ]
    },
    "isolatedTrafficException": {
      "description": "The status of the isolated endpoint network traffic setting.",
      "type": "string",
      "enum": [
        "enabled",
        "disabled"
      ]
    }
  },
  "required": []
}