{
  "type": "object",
  "properties": {
    "id": {
      "description": "The ID of the local application on the Trend Vision One platform",
      "type": "string",
      "maxLength": 128,
      "minLength": 1
    }
  },
  "required": [
    "id"
  ]
}