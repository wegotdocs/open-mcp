{
  "type": "object",
  "properties": {
    "id": {
      "description": "The ID of the AWS account.",
      "type": "string",
      "maxLength": 12
    },
    "If-Match": {
      "description": "Entity tag in MD5 format to perform version control on the resource to be updated. This is returned in response header `ETag` of the v3 GET method.",
      "type": "string",
      "maxLength": 254
    }
  },
  "required": [
    "id"
  ]
}