import { z } from "zod"

export const inputParamsSchema = {
  "TMV1-Filter": z.string().max(2048).describe("Filter for retrieving a subset of the network traffic exceptions list.\n\nSupported fields:\n\n|Field     |Description                                                |Supported values                                 |\n|----------|-----------------------------------------------------------|-------------------------------------------------|\n|direction |The direction of network traffic of the traffic exception. |`inbound`, `outbound`                            |\n|ip        |The IP address of the traffic exception.                   |Any IPv4 address                                 |\n|protocol  |The network protocol of the traffic exception.             |`\"Any\"`, `\"TCP/UDP\"`, `\"TCP\"`, `\"UDP\"`, `\"ICMP\"` |\n|port      |The port numbers associated with the traffic exception.    |0-65535                                          |\n\nSupported operators:\n\n|Operator|Description|\n|-----|-----------|\n|`eq` |Operator 'equal to'|\n|`and`|Operator 'and'|\n|`or` |Operator 'or'|\n|`not`|Operator 'not'|\n|`()` |Symbols for grouping operands with their correct operator|\n").optional()
}