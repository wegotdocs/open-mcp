{
  "type": "object",
  "properties": {
    "id": {
      "description": "The ID of the endpoint on the Trend Vision One platform.",
      "maxLength": 128,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "id"
  ]
}