{
  "type": "object",
  "properties": {
    "orderBy": {
      "description": "Data is displayed in ascending order by default. To specify an order, add the operators asc or desc after a field name.\n\nSupported fields and operators: \n\n* `policyName`\n* `updatedDateTime`\n* `asc` - Ascending order\n* `desc` - Descending order\n",
      "type": "string",
      "maxLength": 128,
      "default": "policyName asc"
    },
    "top": {
      "description": "The number of records displayed on a page.",
      "type": "integer",
      "enum": [
        50,
        100,
        200
      ],
      "default": 100
    }
  },
  "required": []
}