{
  "type": "object",
  "properties": {
    "id": {
      "description": "The GCP project number Cloud Account Management uses for managing the connected GCP project.",
      "type": "string",
      "maxLength": 30
    }
  },
  "required": [
    "id"
  ]
}