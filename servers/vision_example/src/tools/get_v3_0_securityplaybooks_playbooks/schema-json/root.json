{
  "type": "object",
  "properties": {
    "top": {
      "description": "The number of items to return in the result set. Default is 10.",
      "enum": [
        10,
        20,
        50,
        100
      ],
      "type": "integer",
      "default": 10
    },
    "orderBy": {
      "description": "\nSpecifies the field by which the results are sorted.\n\nData is displayed in descending order by default.\n\nSupported fields and operators:\n* 'updatedDateTime'\n* asc - Ascending order\n* desc - Descending order\n\nNote: You can use multiple fields, separated by commas, to sort the retrieved playbooks.\n",
      "type": "string",
      "default": "updatedDateTime desc",
      "maxLength": 100
    },
    "TMV1-Filter": {
      "description": "Filter for retrieving a subset of playbooks.\n\nSupported fields:\n\n|Field|Description|Supported values|\n|-----|-----------|----------------|\n|`type`|The playbook type| activityAndBehavior, general, systemConfiguration, securityPolicy, threatDetection, vulnerability, xdrDetection |\n|`name`|The name of playbook|Any value|\n|`creator`|User that created a playbook|Any value|\n\n\nSupported operators:\n\n|Operator|Description|Notes|\n|-----|-----------|----------------|\n|`eq`|Operator 'equal to'| - |\n|`and`|Operator 'and'| - |\n|`or`|Operator 'or'| - |\n|`not`|Operator 'not'| - |\n|`( )`|Symbols for grouping operands with their correct operator.| - |\n\nAdditional functions:\n\n|Function|Description|Notes|\n|-----|-----------|----------------|\n|`contains()`|Allows you to search for a specified string in a field|Only applicable to 'name'|\n\nNote: Include this parameter in every request that generates paginated output.\n",
      "type": "string",
      "maxLength": 1024
    }
  },
  "required": []
}