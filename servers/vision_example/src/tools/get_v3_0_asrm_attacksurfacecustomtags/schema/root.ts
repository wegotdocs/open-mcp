import { z } from "zod"

export const inputParamsSchema = {
  "top": z.union([z.literal(10), z.literal(50), z.literal(100), z.literal(500), z.literal(1000)]).describe("The number of records displayed on a page. \n\nDefault: `100`\n").optional(),
  "orderBy": z.string().max(128).describe("The field by which the results are sorted.\n\nTo display records in ascending or descending order, add the phrase `asc` or `desc` after the parameter name.\n\nAvailable values: \n* `key`\n* `asc` - Ascending order\n* `desc` - Descending order\n").optional(),
  "TMV1-Filter": z.string().max(1024).describe("The filter for retrieving a subset of the cloud asset information list.\n\nSupported fields:\n\n| Field  | Description                                     | Supported values     |\n| ------ | ------------------------------------------------| -------------------- |\n|id      |The tag ID of each asset in `assetCustomTags`    | Any value in field   |\n|key     |The key of each asset in `assetCustomTags`       | Any value in field   |\n|value   |The tag value of each asset in `assetCustomTags` | Any value in field   |\nSupported operators:\n\n| Operator | Description                                               |\n| -------- | --------------------------------------------------------- |\n| `eq`     | Operator 'equal to'                                       |\n| `and`    | Operator 'and'                                            |\n| `or`     | Operator 'or'                                             |\n| `not`    | Operator 'not'                                            |\n| `()`     | Symbols for grouping operands with their correct operator |\n").optional()
}