{
  "type": "object",
  "properties": {
    "business_id": {
      "description": "Business ID",
      "type": "string"
    },
    "start_date": {
      "description": "Start date for clients balance",
      "type": "string",
      "format": "date-time"
    },
    "end_date": {
      "description": "End date for clients balance",
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "business_id"
  ]
}