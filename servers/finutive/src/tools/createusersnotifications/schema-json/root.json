{
  "type": "object",
  "properties": {
    "sendById": {
      "type": "string"
    },
    "text": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "TALK",
        "LABORAL_FLOW",
        "SERVICE_REQUEST",
        "SERVICE_OPPORTUNITY"
      ]
    },
    "hash": {
      "type": "string"
    },
    "silentPush": {
      "type": "boolean"
    },
    "extraInfo": {
      "type": "object",
      "description": "<llm-instruction>This part of the input schema is truncated. If you want to pass the property `extraInfo` to the tool, first call the tool `expandSchema` with \"/properties/extraInfo\" in the list of pointers. This will return the expanded input schema which you can then use in the tool call. You may have to call `expandSchema` multiple times if the schema is nested.</llm-instruction>",
      "additionalProperties": true
    },
    "userIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "sendById",
    "text",
    "type",
    "userIds"
  ]
}