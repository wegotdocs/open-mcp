{
  "type": "object",
  "properties": {
    "businessId": {
      "description": "Business ID",
      "type": "string"
    },
    "invoiceIds": {
      "description": "Comma-separated list of invoice IDs",
      "type": "string"
    }
  },
  "required": [
    "businessId",
    "invoiceIds"
  ]
}