{
  "type": "object",
  "properties": {
    "date": {
      "description": "Date for which exchange rates are required (ISO format: YYYY-MM-DD)",
      "type": "string",
      "format": "date"
    },
    "base_currency": {
      "description": "ISO code of the base currency",
      "type": "string"
    },
    "force": {
      "description": "Force update from external provider instead of using cached data",
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "date",
    "base_currency"
  ]
}