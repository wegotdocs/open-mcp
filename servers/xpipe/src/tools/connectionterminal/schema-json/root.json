{
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "The optional directory to use as the working directory"
    },
    "connection": {
      "type": "string",
      "description": "The connection uuid"
    }
  },
  "required": [
    "directory",
    "connection"
  ]
}