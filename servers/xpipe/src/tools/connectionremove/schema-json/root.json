{
  "type": "object",
  "properties": {
    "connections": {
      "type": "array",
      "description": "The connections to remove",
      "items": {
        "type": "string",
        "description": "The unique id of the connection"
      }
    }
  },
  "required": [
    "connections"
  ]
}