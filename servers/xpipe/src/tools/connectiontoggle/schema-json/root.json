{
  "type": "object",
  "properties": {
    "state": {
      "type": "boolean",
      "description": "The state to switch to"
    },
    "connection": {
      "type": "string",
      "description": "The connection uuid"
    }
  },
  "required": [
    "state",
    "connection"
  ]
}