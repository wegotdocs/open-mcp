{
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "The optional directory to browse to"
    },
    "connection": {
      "type": "string",
      "description": "The connection uuid"
    }
  },
  "required": [
    "directory",
    "connection"
  ]
}