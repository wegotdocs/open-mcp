{
  "type": "object",
  "properties": {
    "out_request_no": {
      "description": "外部请求号，由商家自定义。32个字符以内，仅可包含字母、数字、下划线。需保证在商户端不重复。",
      "example": "2019032200000001",
      "type": "string"
    },
    "page_num": {
      "description": "几页，起始页为 1。不填默认为 1。",
      "example": 1,
      "type": "integer"
    },
    "page_size": {
      "description": "页面大小。每页记录数，取值范围是(0,100]。不填默认为20",
      "example": 20,
      "type": "integer"
    }
  },
  "required": []
}