import { z } from "zod"

export const inputParams = {
  "cities": z.array(z.string()).describe("报名提交的活动城市码，详见<a href=\"https://opendocs.alipay.com/pre-open/02r07u\" target=\"_blank\">活动城市码</a>").optional(),
  "enroll_merchant": z.record(z.any()).describe("<llm-instruction>This part of the input schema is truncated. If you want to pass the property `enroll_merchant` to the tool, first call the tool `expandSchema` with \"/properties/enroll_info/properties/enroll_merchant\" in the list of pointers. This will return the expanded input schema which you can then use in the tool call. You may have to call `expandSchema` multiple times if the schema is nested.</llm-instruction>").optional(),
  "materials": z.array(z.object({ "data": z.string().describe("提报的素材内容，json字符串，使用时需要把此字符串解析成json对象。\t字段来源于“招商方案查询接口（https://opendocs.alipay.com/pre-open/02bhl7）”中返回的素材字段field。如：activityImage、contentId等。\t字段类型mediaType包含：\t IMAGE，图片类型\t TEXT，文本类型\t DATASOURCE，数据源类型， 如：内容ID（contentId）字段类型为DATASOURCE\t字段取值：\t 图片（IMAGE）：\t aftsFileId：“营销图片资源上传接口（https://opendocs.alipay.com/pre-open/02bhlj）”返回的图片唯一资源ID\t 文本（TEXT）：\t text：文本内容\t 数据源（DATASOURCE）：\t text：当前仅支持内容ID值，可通过“内容批量查询接口（https://opendocs.alipay.com/pre-apis/02lu12，仅供受邀用户使用）”查询内容ID。").optional(), "description": z.string().describe("对这个素材的描述，用于后续管理该素材").optional(), "material_audit_info": z.string().describe("素材审核信息，展示给商家的素材审核不通过的意见。素材审核通过时该字段为空。\t报名详情查询接口不用填写该字段。").optional(), "name": z.string().describe("素材名名称").optional(), "subject_id": z.string().describe("关联的报名主体ID。\tenroll_scene_type是VOUCHER时，报名主体ID是activity_id；enroll_scene_type是MINI_APP时，报名主体ID是mini_app_id").optional() })).describe("报名提交的素材").optional(),
  "mini_apps": z.array(z.object({ "mini_app_id": z.string().describe("小程序ID").optional() })).describe("报名提交的小程序信息，是否必选取决于方案要求").optional(),
  "vouchers": z.array(z.object({ "activity_id": z.string().describe("券活动id。支持商家券（创建商家券请参考https://opendocs.alipay.com/apis/01xm17）和支付券（创建支付券请参考https://opendocs.alipay.com/pre-apis/027185，仅供受邀用户使用），取值均为返回结果的activity_id字段。").optional() })).describe("报名提交的券信息，是否必选取决于方案要求").optional()
}