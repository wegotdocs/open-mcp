import { z } from "zod"

export const inputParams = {
  "settle_detail_infos": z.array(z.object({ "actual_amount": z.string().describe("仅在直付通账期模式下，当一笔交易需要分多次发起部分确认结算时使用，表示本次确认结算的实际结算金额。传递本字段后，原amount字段不再生效，结算金额以本字段为准。如已经发生过部分确认结算、不传递本字段则默认按剩余待结算金额一次性结算。").optional(), "amount": z.string().describe("结算的金额，单位为元。在创建订单和支付接口时必须和交易金额相同。在结算确认接口时必须等于交易金额减去已退款金额。直付通账期模式下，如使用部分结算能力、传递了actual_amount字段，则忽略本字段的校验、可不传。").optional(), "settle_entity_id": z.string().describe("结算主体标识。当结算主体类型为SecondMerchant时，为二级商户的SecondMerchantID；当结算主体类型为Store时，为门店的外标。").optional(), "settle_entity_type": z.string().describe("结算主体类型。").optional(), "summary_dimension": z.string().describe("结算汇总维度，按照这个维度汇总成批次结算，由商户指定。\t\t目前需要和结算收款方账户类型为cardAliasNo配合使用").optional(), "trans_in": z.string().describe("结算收款方。当结算收款方类型是cardAliasNo时，本参数为用户在支付宝绑定的卡编号；结算收款方类型是userId时，本参数为用户的支付宝账号对应的支付宝唯一用户号，以2088开头的纯16位数字；当结算收款方类型是loginName时，本参数为用户的支付宝登录号；当结算收款方类型是defaultSettle时，本参数不能传值，保持为空。").optional(), "trans_in_type": z.string().describe("结算收款方的账户类型。").optional() })).describe("结算详细信息，json数组，目前只支持一条。").optional(),
  "settle_period_time": z.string().describe("该笔订单的超期自动确认结算时间，到达期限后，将自动确认结算。此字段只在签约账期结算模式时有效。取值范围：1d～365d。d-天。 该参数数值不接受小数点。").optional()
}