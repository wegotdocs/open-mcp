{
  "type": "object",
  "properties": {
    "page_num": {
      "description": "查询结果的页码，起始值为 1，默认值为 1",
      "example": 1,
      "type": "integer"
    },
    "page_size": {
      "description": "分页查询时设置的每页记录数，最大值 100 行，默认为 10",
      "example": 10,
      "type": "integer"
    }
  },
  "required": []
}