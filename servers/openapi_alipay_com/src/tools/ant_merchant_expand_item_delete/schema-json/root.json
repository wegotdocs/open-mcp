{
  "type": "object",
  "properties": {
    "item_id": {
      "description": "商品ID（item_id不为空则以item_id作为删除KEY,item_id为空则以external_item_id作为删除KEY,两者不能同时为空）",
      "example": "2018091300502200002600104169",
      "type": "string"
    },
    "external_item_id": {
      "description": "外部商品ID（item_id不为空则以item_id作为删除KEY,item_id为空则以external_item_id作为删除KEY,两者不能同时为空）",
      "example": "20190916123124123123123",
      "type": "string"
    }
  },
  "required": []
}