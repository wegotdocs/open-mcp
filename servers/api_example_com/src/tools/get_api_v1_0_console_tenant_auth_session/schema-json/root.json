{
  "type": "object",
  "properties": {
    "tenant": {
      "description": "tenant use default if not known",
      "type": "string",
      "title": "String"
    }
  },
  "required": [
    "tenant"
  ]
}