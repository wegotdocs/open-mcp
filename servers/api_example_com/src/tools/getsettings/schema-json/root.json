{
  "type": "object",
  "properties": {
    "useCaseId": {
      "description": "Id if use case, this parameter is optional.",
      "type": "string",
      "format": "uuid"
    },
    "showAsArray": {
      "description": "If true, it displays the result as a list of key-value pairs \r\n            and if false an object with the key as the variable name and its value.",
      "type": "boolean"
    }
  },
  "required": []
}