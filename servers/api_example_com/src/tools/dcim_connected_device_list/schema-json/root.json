{
  "type": "object",
  "properties": {
    "peer_device": {
      "description": "The name of the peer device",
      "type": "string"
    },
    "peer_interface": {
      "description": "The name of the peer interface",
      "type": "string"
    }
  },
  "required": [
    "peer_device",
    "peer_interface"
  ]
}