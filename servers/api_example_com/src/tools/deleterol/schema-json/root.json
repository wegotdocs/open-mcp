{
  "type": "object",
  "properties": {
    "id": {
      "description": "The identifier of the role to delete.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ]
}