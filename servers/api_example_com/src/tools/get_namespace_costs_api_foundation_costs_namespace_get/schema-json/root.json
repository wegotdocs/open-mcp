{
  "type": "object",
  "properties": {
    "namespace": {
      "type": "string",
      "title": "Namespace"
    },
    "use_cache": {
      "description": "Use cached data if available",
      "type": "boolean",
      "default": true,
      "title": "Use Cache"
    }
  },
  "required": [
    "namespace"
  ]
}