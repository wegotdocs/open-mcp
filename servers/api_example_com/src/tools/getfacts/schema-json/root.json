{
  "type": "object",
  "properties": {
    "max_length": {
      "description": "maximum length of returned fact",
      "type": "integer",
      "format": "int64"
    },
    "limit": {
      "description": "limit the amount of results returned",
      "type": "integer",
      "format": "int64"
    }
  },
  "required": []
}