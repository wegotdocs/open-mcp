{
  "type": "object",
  "properties": {
    "older_than_hours": {
      "description": "Remove completed tasks older than this many hours",
      "type": "integer",
      "default": 24,
      "title": "Older Than Hours"
    }
  },
  "required": []
}