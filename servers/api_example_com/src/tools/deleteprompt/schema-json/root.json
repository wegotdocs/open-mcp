{
  "type": "object",
  "properties": {
    "id": {
      "description": "Id in Guid format for identify prompt",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ]
}