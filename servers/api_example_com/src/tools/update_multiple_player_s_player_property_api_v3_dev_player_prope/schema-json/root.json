{
  "type": "object",
  "properties": {
    "session_id": {
      "type": "string",
      "title": "Session Id"
    },
    "dev_id_list": {
      "items": {
        "type": "integer"
      },
      "type": "array",
      "title": "Dev Id List",
      "description": "播放器 ID 清單。"
    },
    "dev_player_property_in": {
      "type": "object",
      "description": "<llm-instruction>This part of the input schema is truncated. If you want to pass the property `dev_player_property_in` to the tool, first call the tool `expandSchema` with \"/properties/dev_player_property_in\" in the list of pointers. This will return the expanded input schema which you can then use in the tool call. You may have to call `expandSchema` multiple times if the schema is nested.</llm-instruction>\n<property-description>播放器屬性。</property-description>",
      "additionalProperties": true
    }
  },
  "required": [
    "dev_id_list",
    "dev_player_property_in"
  ]
}