{
  "type": "object",
  "properties": {
    "org_id": {
      "description": "Only the super administrator can specify the org_id; otherwise, the org_id will default to the user's org_id.",
      "type": "integer",
      "title": "Org Id"
    },
    "brief": {
      "description": "Brief data only? Empty / 0: No, Any / 1: Yes",
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "title": "Brief"
    },
    "session_id": {
      "type": "string",
      "title": "Session Id"
    }
  },
  "required": [
    "org_id"
  ]
}