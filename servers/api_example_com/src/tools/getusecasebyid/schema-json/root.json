{
  "type": "object",
  "properties": {
    "id": {
      "description": "Id in Guid format for identify useCase",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ]
}