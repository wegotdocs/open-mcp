{
  "type": "object",
  "properties": {
    "id": {
      "description": "The identifier of the role to update.",
      "type": "string",
      "format": "uuid"
    },
    "rolName": {
      "type": "string",
      "description": "Rol Name",
      "nullable": true
    }
  },
  "required": [
    "id"
  ]
}