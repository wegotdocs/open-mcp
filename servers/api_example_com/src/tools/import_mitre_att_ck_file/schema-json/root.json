{
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512
    },
    "catalog": {
      "type": "string",
      "description": "Either a name or id of an existing catalog in TheHive or if a catalog with this name does not exists, one will be created",
      "minLength": 1,
      "maxLength": 128
    },
    "variant": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  },
  "required": [
    "catalog"
  ]
}