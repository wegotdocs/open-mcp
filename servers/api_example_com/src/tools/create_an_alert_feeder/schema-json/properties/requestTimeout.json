{
  "description": "The maximum amount of time the request is allowed to take",
  "default": "Interval(10,Seconds)",
  "required": [
    "value",
    "unit"
  ],
  "type": "object",
  "properties": {
    "value": {
      "type": "integer",
      "format": "int32",
      "exclusiveMinimum": 0
    },
    "unit": {
      "type": "string",
      "enum": [
        "Days",
        "Hours",
        "Minutes",
        "Seconds",
        "Milliseconds"
      ]
    }
  }
}